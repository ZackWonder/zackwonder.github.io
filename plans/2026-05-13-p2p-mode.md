# 四子棋 P2P 模式实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在不引入后端的前提下，给 zackwonder.github.io 的四子棋加上 P2P 在线对战模式：发起方选阵营 → 复制邀请链接 → 对方打开链接 → 立即对战。

**Architecture:** 把现有 `Game.tsx` 拆成纯函数层（`game/gameLogic.ts`）、容器层（`game/GameContainer.tsx`）、视图层（`game/GameView.tsx`），并新增 `p2p/` 模块（`protocol.ts` + `usePeerConnection.ts` + `InviteModal.tsx`）。P2P 走 PeerJS 默认公共 broker 做信令，两端各自演算游戏状态，只通过 RTCDataChannel 同步"动作"（落子列号、重开）。

**Tech Stack:** React 19、TypeScript、Vite 6、PeerJS 1.5.x；测试用 Vitest + @testing-library/react + happy-dom（新增）。

**Reference Spec:** `specs/2026-05-13-p2p-mode-design.md`

---

### Task 0: 安装测试依赖并配置 Vitest

**Files:**
- Modify: `package.json`
- Modify: `vite.config.ts`
- Create: `src/test-setup.ts`
- Create: `vitest.config.ts`（或合并进 vite.config.ts）

- [ ] **Step 1: 安装依赖**

```bash
cd /Users/zack/Documents/bitabc/research/zackwonder.github.io
npm install --save-dev vitest @testing-library/react @testing-library/jest-dom happy-dom @types/node
```

Expected: `package.json` 出现 5 个新 devDependencies。

- [ ] **Step 2: 改写 `vite.config.ts` 加入 test 配置**

```ts
/// <reference types="vitest" />
import { defineConfig } from "vite";
import react from "@vitejs/plugin-react";

export default defineConfig({
  plugins: [react()],
  build: {
    outDir: "docs",
  },
  test: {
    environment: "happy-dom",
    globals: true,
    setupFiles: ["./src/test-setup.ts"],
  },
});
```

- [ ] **Step 3: 创建 `src/test-setup.ts`**

```ts
import "@testing-library/jest-dom/vitest";
```

- [ ] **Step 4: 在 `package.json` scripts 加入 `test`**

```json
{
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  }
}
```

- [ ] **Step 5: 跑一次空测试确认配置正常**

```bash
npm test
```

Expected: `No test files found, exiting with code 0` 或类似（vitest 找不到测试文件但不报错）。如报错则配置有问题，需修。

- [ ] **Step 6: 提交**

```bash
git add package.json package-lock.json vite.config.ts src/test-setup.ts
git commit -m "chore: add vitest + testing-library setup"
```

---

### Task 1: 创建 `src/game/types.ts`

**Files:**
- Create: `src/game/types.ts`

- [ ] **Step 1: 写出类型定义**

```ts
import type { Player } from "../constants";

export type Point = [number, number];

export type Winner = Player | 3;

export type Cell = Player | undefined;

export type Board = Cell[][];

export interface GameState {
  squares: Board;
  history: Point[];
  aTurn: boolean;
  aIsStarter: boolean;
  winner: Winner | undefined;
  winPoints: Point[] | undefined;
  droppingCell: Point | null;
}
```

- [ ] **Step 2: 提交**

```bash
git add src/game/types.ts
git commit -m "feat(game): extract shared game types"
```

---

### Task 2: TDD 抽出纯游戏逻辑 `src/game/gameLogic.ts`

**Files:**
- Create: `src/game/gameLogic.ts`
- Create: `src/game/gameLogic.test.ts`

- [ ] **Step 1: 先写完整测试文件 (RED)**

```ts
// src/game/gameLogic.test.ts
import { describe, expect, it } from "vitest";
import constants from "../constants";
import {
  createEmptyBoard,
  dropInColumn,
  linePoints,
  isLine4,
  createInitialState,
  applyMove,
  applyReset,
} from "./gameLogic";

describe("createEmptyBoard", () => {
  it("returns WIDTH columns each of HEIGHT undefined cells", () => {
    const b = createEmptyBoard();
    expect(b).toHaveLength(constants.WIDTH);
    expect(b[0]).toHaveLength(constants.HEIGHT);
    expect(b.every((col) => col.every((c) => c === undefined))).toBe(true);
  });
});

describe("dropInColumn", () => {
  it("places the player in the first empty slot and returns its row", () => {
    const col = [undefined, undefined, undefined, undefined, undefined, undefined];
    const row = dropInColumn(constants.PLAYER_A, col);
    expect(row).toBe(0);
    expect(col[0]).toBe(constants.PLAYER_A);
  });

  it("stacks on top of existing pieces", () => {
    const col = [constants.PLAYER_A, undefined, undefined, undefined, undefined, undefined];
    const row = dropInColumn(constants.PLAYER_B, col);
    expect(row).toBe(1);
    expect(col[1]).toBe(constants.PLAYER_B);
  });

  it("returns undefined when column is full", () => {
    const col = Array(constants.HEIGHT).fill(constants.PLAYER_A);
    const row = dropInColumn(constants.PLAYER_B, col);
    expect(row).toBeUndefined();
  });
});

describe("isLine4", () => {
  it("detects 4 in a row horizontally", () => {
    const board = createEmptyBoard();
    for (let i = 0; i < 4; i++) board[i]![0] = constants.PLAYER_A;
    expect(isLine4(board, 3, 0)).toBeDefined();
  });

  it("detects 4 in a row vertically", () => {
    const board = createEmptyBoard();
    for (let j = 0; j < 4; j++) board[0]![j] = constants.PLAYER_A;
    expect(isLine4(board, 0, 3)).toBeDefined();
  });

  it("detects 4 in a row diagonal /", () => {
    const board = createEmptyBoard();
    for (let k = 0; k < 4; k++) board[k]![k] = constants.PLAYER_A;
    expect(isLine4(board, 3, 3)).toBeDefined();
  });

  it("detects 4 in a row diagonal \\", () => {
    const board = createEmptyBoard();
    for (let k = 0; k < 4; k++) board[k]![3 - k] = constants.PLAYER_A;
    expect(isLine4(board, 3, 0)).toBeDefined();
  });

  it("returns undefined when there's no line of 4", () => {
    const board = createEmptyBoard();
    board[0]![0] = constants.PLAYER_A;
    expect(isLine4(board, 0, 0)).toBeUndefined();
  });
});

describe("linePoints", () => {
  it("returns 1 point when standalone", () => {
    const board = createEmptyBoard();
    board[0]![0] = constants.PLAYER_A;
    expect(linePoints(board, 0, 0, 1, 0)).toHaveLength(1);
  });
});

describe("applyMove", () => {
  it("places a piece and toggles turn on a valid move", () => {
    const s = createInitialState(true);
    const next = applyMove(s, 0);
    expect(next).not.toBe(s); // immutable
    expect(next.squares[0]![0]).toBe(constants.PLAYER_A);
    expect(next.aTurn).toBe(false);
    expect(next.history).toEqual([[0, 0]]);
    expect(next.droppingCell).toEqual([0, 0]);
  });

  it("returns the same state when the column is full", () => {
    let s = createInitialState(true);
    for (let i = 0; i < constants.HEIGHT; i++) s = applyMove(s, 0);
    const full = applyMove(s, 0);
    expect(full).toBe(s);
  });

  it("detects a horizontal win", () => {
    let s = createInitialState(true);
    // A B A B A B A A wins on column 3 row 0? Let's just A four in a row col 0..3
    // Simulate: A col0, B col0, A col1, B col1, A col2, B col2, A col3
    s = applyMove(s, 0); // A (0,0)
    s = applyMove(s, 0); // B (0,1)
    s = applyMove(s, 1); // A (1,0)
    s = applyMove(s, 1); // B (1,1)
    s = applyMove(s, 2); // A (2,0)
    s = applyMove(s, 2); // B (2,1)
    s = applyMove(s, 3); // A (3,0) -> win
    expect(s.winner).toBe(constants.PLAYER_A);
    expect(s.winPoints).toBeDefined();
  });

  it("locks the board after a winner is set", () => {
    let s = createInitialState(true);
    // Quick win: col 0 vertically
    s = applyMove(s, 0);
    s = applyMove(s, 1);
    s = applyMove(s, 0);
    s = applyMove(s, 1);
    s = applyMove(s, 0);
    s = applyMove(s, 1);
    s = applyMove(s, 0); // A wins vertically in col 0
    expect(s.winner).toBe(constants.PLAYER_A);
    const after = applyMove(s, 2);
    expect(after).toBe(s);
  });
});

describe("applyReset", () => {
  it("alternates starter on reset", () => {
    let s = createInitialState(true);
    s = applyMove(s, 0);
    const reset = applyReset(s);
    expect(reset.aIsStarter).toBe(false);
    expect(reset.aTurn).toBe(false);
    expect(reset.squares).toEqual(createEmptyBoard());
    expect(reset.history).toEqual([]);
    expect(reset.winner).toBeUndefined();
  });
});
```

- [ ] **Step 2: 跑测试确认全部 FAIL**

```bash
npm test
```

Expected: 多个测试失败（模块未实现）。

- [ ] **Step 3: 实现 `src/game/gameLogic.ts`**

```ts
import constants, { type Player } from "../constants";
import type { Board, Cell, GameState, Point, Winner } from "./types";

export function createEmptyBoard(): Board {
  return Array.from({ length: constants.WIDTH }, () =>
    new Array<Cell>(constants.HEIGHT).fill(undefined)
  );
}

export function dropInColumn(player: Player, col: Cell[]): number | undefined {
  for (let i = 0; i < col.length; i++) {
    if (col[i] === undefined) {
      col[i] = player;
      return i;
    }
  }
  return undefined;
}

export function linePoints(
  square: Board,
  x1: number,
  y1: number,
  dx: number,
  dy: number
): Point[] {
  const points: Point[] = [[x1, y1]];
  const v = square[x1]![y1];
  for (
    let i = x1 + dx, j = y1 + dy;
    i < constants.WIDTH && i >= 0 && j < constants.HEIGHT && j >= 0;
    i += dx, j += dy
  ) {
    if (square[i]![j] === v) points.push([i, j]);
    else break;
  }
  for (
    let i = x1 - dx, j = y1 - dy;
    i < constants.WIDTH && i >= 0 && j < constants.HEIGHT && j >= 0;
    i -= dx, j -= dy
  ) {
    if (square[i]![j] === v) points.push([i, j]);
    else break;
  }
  return points;
}

export function isLine4(square: Board, x1: number, y1: number): Point[] | undefined {
  const directions: [number, number][] = [
    [1, 0],
    [0, 1],
    [1, 1],
    [1, -1],
  ];
  for (const [dx, dy] of directions) {
    const points = linePoints(square, x1, y1, dx, dy);
    if (points.length >= 4) return points;
  }
  return undefined;
}

export function createInitialState(aIsStarter: boolean): GameState {
  return {
    squares: createEmptyBoard(),
    history: [],
    aTurn: aIsStarter,
    aIsStarter,
    winner: undefined,
    winPoints: undefined,
    droppingCell: null,
  };
}

export function applyMove(state: GameState, columnIndex: number): GameState {
  if (state.winner !== undefined) return state;
  if (state.squares[columnIndex]?.[constants.HEIGHT - 1] !== undefined) return state;

  const newSquares = state.squares.map((c) => [...c]);
  const player = state.aTurn ? constants.PLAYER_A : constants.PLAYER_B;
  const droppedJ = dropInColumn(player, newSquares[columnIndex]!);
  if (droppedJ === undefined) return state;

  const newHistory: Point[] = [...state.history, [columnIndex, droppedJ]];
  const newWinPoints = isLine4(newSquares, columnIndex, droppedJ);

  let newWinner: Winner | undefined = undefined;
  if (newWinPoints) newWinner = player;
  else if (newSquares.every((col) => col[constants.HEIGHT - 1] !== undefined))
    newWinner = 3;

  return {
    ...state,
    squares: newSquares,
    history: newHistory,
    aTurn: !state.aTurn,
    winner: newWinner,
    winPoints: newWinPoints,
    droppingCell: [columnIndex, droppedJ],
  };
}

export function applyReset(state: GameState): GameState {
  return createInitialState(!state.aIsStarter);
}

export function applyUndo(state: GameState): GameState {
  if (state.history.length === 0) return state;
  const last = state.history[state.history.length - 1]!;
  const newSquares = state.squares.map((c) => [...c]);
  newSquares[last[0]]![last[1]] = undefined;
  return {
    ...state,
    squares: newSquares,
    history: state.history.slice(0, -1),
    aTurn: !state.aTurn,
    droppingCell: null,
  };
}
```

- [ ] **Step 4: 跑测试确认全部 PASS**

```bash
npm test
```

Expected: 所有 `gameLogic.test.ts` 测试通过。

- [ ] **Step 5: 提交**

```bash
git add src/game/gameLogic.ts src/game/gameLogic.test.ts
git commit -m "feat(game): extract pure game logic with unit tests"
```

---

### Task 3: 创建 `GameView.tsx`（presentational）

**Files:**
- Create: `src/game/GameView.tsx`

- [ ] **Step 1: 写 `GameView.tsx`**

```tsx
import { useEffect } from "react";
import Board from "../Board";
import PlayAffect from "../effect";
import constants from "../constants";
import type { GameState } from "./types";
import type { Winner } from "./types";

interface CrownProps {
  value: Winner | undefined;
  handlePlayAgain: () => void;
}

function Crown({ value, handlePlayAgain }: CrownProps) {
  useEffect(() => {
    if (value) {
      const btn = document.querySelector(".crownSpan");
      if (btn instanceof HTMLElement) PlayAffect(btn);
    }
  }, [value]);

  if (!value) return null;

  if (value !== 3) {
    const cn = value === constants.PLAYER_A ? "playerA crown" : "playerB crown";
    return (
      <div className="crownDiv">
        <span className="crownSpan">
          <button className={cn}></button>
        </span>
        <br />
        贏了！
        <br />
        <button id="playAgainBtn" onClick={handlePlayAgain}>
          再玩一次
        </button>
        <hr />
      </div>
    );
  }

  return (
    <div className="crownDiv">
      <span className="crownSpan">
        <button className="playerA crown"></button>
        <button className="playerB crown"></button>
      </span>
      <br />
      <button id="playAgainBtn" onClick={handlePlayAgain}>
        再玩一次
      </button>
      <hr />
    </div>
  );
}

export interface GameViewProps {
  state: GameState;
  aLevel: number;
  bLevel: number;
  onColumnClick: (col: number) => void;
  onPlayAgain: () => void;
  onUndo?: () => void;            // 不传则不渲染悔棋按钮（P2P 模式）
  topBanner?: React.ReactNode;    // P2P 状态条等额外信息
  extraControls?: React.ReactNode; // 例如"P2P 对战"按钮
}

export default function GameView({
  state,
  aLevel,
  bLevel,
  onColumnClick,
  onPlayAgain,
  onUndo,
  topBanner,
  extraControls,
}: GameViewProps) {
  const lastStep = state.history.length > 0 ? state.history[state.history.length - 1] : undefined;
  return (
    <div
      className="App"
      style={{
        ["--playerA-img" as never]: `url('/tanson_${aLevel}.jpg')`,
        ["--playerB-img" as never]: `url('/sherly_${bLevel}.jpg')`,
      } as React.CSSProperties}
    >
      <header className="App-header">
        {topBanner}
        <Crown value={state.winner} handlePlayAgain={onPlayAgain} />
        <Board
          w={constants.WIDTH}
          h={constants.HEIGHT}
          squares={state.squares}
          lastStep={lastStep}
          droppingCell={state.droppingCell}
          aTurn={state.aTurn}
          winPoints={state.winPoints}
          onClick={(i) => onColumnClick(i)}
        />
        <hr />
        {!state.winner && onUndo && <button onClick={onUndo}>悔棋</button>}
        {extraControls}
      </header>
    </div>
  );
}
```

- [ ] **Step 2: tsc 校验**

```bash
npx tsc -b --noEmit
```

Expected: 无错误。

- [ ] **Step 3: 提交**

```bash
git add src/game/GameView.tsx
git commit -m "feat(game): add presentational GameView component"
```

---

### Task 4: 创建 `GameContainer.tsx`（state owner，单机模式）

> 暂不接 transport，先确认单机模式可工作。Transport 接入在 Task 15。

**Files:**
- Create: `src/game/GameContainer.tsx`

- [ ] **Step 1: 写 `GameContainer.tsx`**

```tsx
import { useCallback, useEffect, useState } from "react";
import constants from "../constants";
import { applyMove, applyReset, applyUndo, createInitialState } from "./gameLogic";
import type { GameState, Winner } from "./types";
import GameView from "./GameView";

function playWinSound(winner: Winner) {
  const soundMap: Record<number, string> = {
    [constants.PLAYER_A]: "./tansonwin.mp3",
    [constants.PLAYER_B]: "./sherlywin.mp3",
    3: "./allwin.mp3",
  };
  const url = soundMap[winner];
  if (url) {
    const audio = new Audio(url);
    audio.volume = 0.25;
    audio.play().catch(() => {});
  }
}

type AvatarSlot = {
  name: "tanson" | "sherly";
  level: number;
  setLevel: (n: number) => void;
};

function probeNextLevel(slot: AvatarSlot): Promise<boolean> {
  return new Promise((resolve) => {
    const img = new Image();
    img.onload = () => resolve(true);
    img.onerror = () => resolve(false);
    img.src = `/${slot.name}_${slot.level + 1}.jpg`;
  });
}

async function tryUpgradeOrReset(winner: AvatarSlot, loser: AvatarSlot): Promise<void> {
  const winnerHasNext = await probeNextLevel(winner);
  if (winnerHasNext) {
    winner.setLevel(winner.level + 1);
    return;
  }
  const loserHasNext = await probeNextLevel(loser);
  if (!loserHasNext) {
    winner.setLevel(0);
    loser.setLevel(0);
  }
}

export interface GameContainerProps {
  extraControls?: React.ReactNode;
  topBanner?: React.ReactNode;
}

export default function GameContainer({ extraControls, topBanner }: GameContainerProps) {
  const [state, setState] = useState<GameState>(() => createInitialState(true));
  const [aLevel, setALevel] = useState(0);
  const [bLevel, setBLevel] = useState(0);

  // 落子后处理胜利音效 + 头像升级（observe 模式：在 state.winner 变化时触发）
  useEffect(() => {
    if (state.winner === undefined) return;
    playWinSound(state.winner);
    if (state.winner === 3) return;
    const slotA: AvatarSlot = { name: "tanson", level: aLevel, setLevel: setALevel };
    const slotB: AvatarSlot = { name: "sherly", level: bLevel, setLevel: setBLevel };
    if (state.winner === constants.PLAYER_A) void tryUpgradeOrReset(slotA, slotB);
    else void tryUpgradeOrReset(slotB, slotA);
  }, [state.winner]); // 故意只 watch winner

  // 清掉 droppingCell 的动画状态
  useEffect(() => {
    if (state.droppingCell === null) return;
    const t = setTimeout(() => {
      setState((s) => (s.droppingCell ? { ...s, droppingCell: null } : s));
    }, 400);
    return () => clearTimeout(t);
  }, [state.droppingCell]);

  const handleColumnClick = useCallback((col: number) => {
    setState((s) => applyMove(s, col));
  }, []);

  const handlePlayAgain = useCallback(() => {
    setState((s) => applyReset(s));
  }, []);

  const handleUndo = useCallback(() => {
    setState((s) => applyUndo(s));
  }, []);

  return (
    <GameView
      state={state}
      aLevel={aLevel}
      bLevel={bLevel}
      onColumnClick={handleColumnClick}
      onPlayAgain={handlePlayAgain}
      onUndo={handleUndo}
      extraControls={extraControls}
      topBanner={topBanner}
    />
  );
}
```

- [ ] **Step 2: tsc 校验**

```bash
npx tsc -b --noEmit
```

Expected: 无错误。

- [ ] **Step 3: 提交**

```bash
git add src/game/GameContainer.tsx
git commit -m "feat(game): add GameContainer single-player state owner"
```

---

### Task 5: 替换 `src/Game.tsx` 改用 `GameContainer`（单机模式不变）

**Files:**
- Modify: `src/Game.tsx`

- [ ] **Step 1: 完全重写 `Game.tsx`**

```tsx
import GameContainer from "./game/GameContainer";

export default function GameApp() {
  return <GameContainer />;
}
```

- [ ] **Step 2: tsc 校验**

```bash
npx tsc -b --noEmit
```

Expected: 无错误。

- [ ] **Step 3: 手测单机模式**

```bash
npm run dev
```

打开 http://localhost:5173 → 点心跳进入游戏（`#game`）→ 完整玩一局 → 落子动画、胜利音效、头像升级、再玩一次、悔棋 都应正常工作。

如有 regression：检查 `useEffect` 触发顺序（droppingCell 动画 与 winner 处理），必要时回 Task 4 调整。

- [ ] **Step 4: 提交**

```bash
git add src/Game.tsx
git commit -m "refactor(game): swap GameApp body for GameContainer"
```

---

### Task 6: TDD `src/p2p/protocol.ts`

**Files:**
- Create: `src/p2p/protocol.ts`
- Create: `src/p2p/protocol.test.ts`

- [ ] **Step 1: 写测试**

```ts
// src/p2p/protocol.test.ts
import { describe, expect, it } from "vitest";
import { buildInviteUrl, parseInviteHash } from "./protocol";

describe("buildInviteUrl", () => {
  it("includes #game prefix and encodes joiner role", () => {
    const u = buildInviteUrl("https://example.com", "abc123", "B");
    expect(u).toBe("https://example.com/#game?peer=abc123&role=red");
  });

  it("encodes joiner=A as blue", () => {
    const u = buildInviteUrl("https://example.com", "xyz", "A");
    expect(u).toBe("https://example.com/#game?peer=xyz&role=blue");
  });
});

describe("parseInviteHash", () => {
  it("parses a valid invite hash with role=red -> joiner is B", () => {
    expect(parseInviteHash("#game?peer=abc&role=red")).toEqual({ peerId: "abc", role: "B" });
  });

  it("parses a valid invite hash with role=blue -> joiner is A", () => {
    expect(parseInviteHash("#game?peer=xyz&role=blue")).toEqual({ peerId: "xyz", role: "A" });
  });

  it("returns null for plain #game", () => {
    expect(parseInviteHash("#game")).toBeNull();
  });

  it("returns null when peer missing", () => {
    expect(parseInviteHash("#game?role=red")).toBeNull();
  });

  it("returns null when role missing", () => {
    expect(parseInviteHash("#game?peer=abc")).toBeNull();
  });

  it("returns null when role is not red/blue", () => {
    expect(parseInviteHash("#game?peer=abc&role=green")).toBeNull();
  });

  it("returns null when hash is not #game prefixed", () => {
    expect(parseInviteHash("#peer=abc&role=red")).toBeNull();
  });

  it("returns null for empty hash", () => {
    expect(parseInviteHash("")).toBeNull();
  });
});

describe("round trip", () => {
  it("buildInviteUrl + parseInviteHash inverts roles correctly", () => {
    for (const joinerRole of ["A", "B"] as const) {
      const url = buildInviteUrl("https://x.test", "id-1", joinerRole);
      const parsed = parseInviteHash("#" + url.split("#")[1]!);
      expect(parsed).toEqual({ peerId: "id-1", role: joinerRole });
    }
  });
});
```

- [ ] **Step 2: 跑测试确认 FAIL**

```bash
npm test
```

Expected: 测试 fail（模块未创建）。

- [ ] **Step 3: 实现 `src/p2p/protocol.ts`**

```ts
export type PlayerRole = "A" | "B";

export type PeerMessage =
  | { type: "move"; col: number; seq: number }
  | { type: "reset"; seq: number }
  | { type: "hello"; role: PlayerRole };

const COLOR_TO_ROLE: Record<string, PlayerRole> = { blue: "A", red: "B" };
const ROLE_TO_COLOR: Record<PlayerRole, "blue" | "red"> = { A: "blue", B: "red" };

export function buildInviteUrl(origin: string, peerId: string, joinerRole: PlayerRole): string {
  return `${origin}/#game?peer=${encodeURIComponent(peerId)}&role=${ROLE_TO_COLOR[joinerRole]}`;
}

export function parseInviteHash(hash: string): { peerId: string; role: PlayerRole } | null {
  if (!hash.startsWith("#game")) return null;
  const qIdx = hash.indexOf("?");
  if (qIdx === -1) return null;
  const params = new URLSearchParams(hash.slice(qIdx + 1));
  const peerId = params.get("peer");
  const color = params.get("role");
  if (!peerId || !color) return null;
  const role = COLOR_TO_ROLE[color];
  if (!role) return null;
  return { peerId, role };
}

export function isPeerMessage(value: unknown): value is PeerMessage {
  if (!value || typeof value !== "object") return false;
  const v = value as { type?: unknown };
  if (typeof v.type !== "string") return false;
  if (v.type === "move") {
    const m = value as { col?: unknown; seq?: unknown };
    return typeof m.col === "number" && typeof m.seq === "number";
  }
  if (v.type === "reset") {
    const m = value as { seq?: unknown };
    return typeof m.seq === "number";
  }
  if (v.type === "hello") {
    const m = value as { role?: unknown };
    return m.role === "A" || m.role === "B";
  }
  return false;
}
```

- [ ] **Step 4: 跑测试确认 PASS**

```bash
npm test
```

Expected: 全部通过。

- [ ] **Step 5: 提交**

```bash
git add src/p2p/protocol.ts src/p2p/protocol.test.ts
git commit -m "feat(p2p): add wire protocol and invite URL helpers"
```

---

### Task 7: 安装 peerjs

**Files:**
- Modify: `package.json`

- [ ] **Step 1: 安装**

```bash
npm install peerjs
```

Expected: `peerjs` 出现在 dependencies。

- [ ] **Step 2: 提交**

```bash
git add package.json package-lock.json
git commit -m "chore: add peerjs dependency"
```

---

### Task 8: 实现 `useHostPeer` + `useJoinPeer`（含 mock 测试）

**Files:**
- Create: `src/p2p/usePeerConnection.ts`
- Create: `src/p2p/usePeerConnection.test.tsx`

- [ ] **Step 1: 写测试（用 vi.mock 替换 peerjs）**

```tsx
// src/p2p/usePeerConnection.test.tsx
import { act, renderHook } from "@testing-library/react";
import { afterEach, beforeEach, describe, expect, it, vi } from "vitest";
import type { PeerMessage } from "./protocol";

type Handler = (...args: unknown[]) => void;

class FakeDataConnection {
  open = true;
  handlers = new Map<string, Handler[]>();
  sent: PeerMessage[] = [];
  on(ev: string, cb: Handler) {
    if (!this.handlers.has(ev)) this.handlers.set(ev, []);
    this.handlers.get(ev)!.push(cb);
  }
  emit(ev: string, payload?: unknown) {
    (this.handlers.get(ev) ?? []).forEach((h) => h(payload));
  }
  send(msg: PeerMessage) {
    this.sent.push(msg);
  }
  close() {
    this.open = false;
    this.emit("close");
  }
}

class FakePeer {
  handlers = new Map<string, Handler[]>();
  destroyed = false;
  constructor(public id?: string) {
    FakePeer.instances.push(this);
  }
  static instances: FakePeer[] = [];
  on(ev: string, cb: Handler) {
    if (!this.handlers.has(ev)) this.handlers.set(ev, []);
    this.handlers.get(ev)!.push(cb);
  }
  emit(ev: string, payload?: unknown) {
    (this.handlers.get(ev) ?? []).forEach((h) => h(payload));
  }
  connect(_: string): FakeDataConnection {
    const conn = new FakeDataConnection();
    this.lastConn = conn;
    return conn;
  }
  lastConn?: FakeDataConnection;
  destroy() {
    this.destroyed = true;
  }
}

vi.mock("peerjs", () => ({ default: FakePeer }));

import { useHostPeer, useJoinPeer } from "./usePeerConnection";

beforeEach(() => {
  FakePeer.instances = [];
});

afterEach(() => {
  vi.clearAllMocks();
});

describe("useHostPeer", () => {
  it("transitions init → awaiting → connected", () => {
    const { result } = renderHook(() => useHostPeer());
    expect(result.current.status).toBe("init");
    const peer = FakePeer.instances[0]!;
    act(() => peer.emit("open", "host-id-1"));
    expect(result.current.status).toBe("awaiting");
    expect(result.current.peerId).toBe("host-id-1");

    const conn = new FakeDataConnection();
    act(() => peer.emit("connection", conn));
    act(() => conn.emit("open"));
    expect(result.current.status).toBe("connected");
  });

  it("rejects a second incoming connection", () => {
    const { result } = renderHook(() => useHostPeer());
    const peer = FakePeer.instances[0]!;
    act(() => peer.emit("open", "host-id-1"));

    const c1 = new FakeDataConnection();
    act(() => peer.emit("connection", c1));
    act(() => c1.emit("open"));

    const c2 = new FakeDataConnection();
    act(() => peer.emit("connection", c2));
    expect(c2.open).toBe(false);
    expect(result.current.status).toBe("connected");
  });

  it("disconnect transitions to disconnected", () => {
    const { result } = renderHook(() => useHostPeer());
    const peer = FakePeer.instances[0]!;
    act(() => peer.emit("open", "host-id-1"));
    const conn = new FakeDataConnection();
    act(() => peer.emit("connection", conn));
    act(() => conn.emit("open"));
    act(() => conn.emit("close"));
    expect(result.current.status).toBe("disconnected");
  });

  it("delivers messages to subscribers and unsubscribes", () => {
    const { result } = renderHook(() => useHostPeer());
    const peer = FakePeer.instances[0]!;
    act(() => peer.emit("open", "host-id-1"));
    const conn = new FakeDataConnection();
    act(() => peer.emit("connection", conn));
    act(() => conn.emit("open"));

    const received: PeerMessage[] = [];
    let unsubscribe: (() => void) | undefined;
    act(() => {
      unsubscribe = result.current.onMessage((m) => received.push(m));
    });
    act(() => conn.emit("data", { type: "move", col: 3, seq: 1 }));
    expect(received).toEqual([{ type: "move", col: 3, seq: 1 }]);

    act(() => unsubscribe!());
    act(() => conn.emit("data", { type: "move", col: 2, seq: 2 }));
    expect(received).toHaveLength(1);
  });

  it("send before connection ready does not throw", () => {
    const { result } = renderHook(() => useHostPeer());
    expect(() => result.current.send({ type: "reset", seq: 1 })).not.toThrow();
  });

  it("cleans up the peer on unmount", () => {
    const { unmount } = renderHook(() => useHostPeer());
    const peer = FakePeer.instances[0]!;
    unmount();
    expect(peer.destroyed).toBe(true);
  });
});

describe("useJoinPeer", () => {
  it("dials the remote peer and transitions to connected", () => {
    const { result } = renderHook(() => useJoinPeer("remote-id"));
    expect(result.current.status).toBe("init");
    const peer = FakePeer.instances[0]!;
    act(() => peer.emit("open", "joiner-self-id"));
    expect(result.current.status).toBe("dialing");
    const conn = peer.lastConn!;
    act(() => conn.emit("open"));
    expect(result.current.status).toBe("connected");
  });

  it("failed peer.error transitions to failed", () => {
    const { result } = renderHook(() => useJoinPeer("remote-id"));
    const peer = FakePeer.instances[0]!;
    act(() => peer.emit("error", { type: "peer-unavailable", message: "no host" }));
    expect(result.current.status).toBe("failed");
  });
});
```

- [ ] **Step 2: 跑测试，确认 FAIL**

```bash
npm test
```

Expected: fail（模块未实现）。

- [ ] **Step 3: 实现 `src/p2p/usePeerConnection.ts`**

```ts
import { useCallback, useEffect, useRef, useState } from "react";
import Peer, { type DataConnection } from "peerjs";
import { isPeerMessage, type PeerMessage } from "./protocol";

export type PeerStatus =
  | "init"
  | "awaiting"
  | "dialing"
  | "connected"
  | "disconnected"
  | "failed";

export interface UsePeerConnectionResult {
  status: PeerStatus;
  peerId: string | null;
  error: string | null;
  send: (msg: PeerMessage) => void;
  onMessage: (handler: (msg: PeerMessage) => void) => () => void;
}

function bindConnection(
  conn: DataConnection,
  listenersRef: React.MutableRefObject<Set<(msg: PeerMessage) => void>>,
  setStatus: (s: PeerStatus) => void,
  setError: (s: string | null) => void
) {
  conn.on("open", () => setStatus("connected"));
  conn.on("data", (data) => {
    if (!isPeerMessage(data)) {
      console.warn("[p2p] dropping unknown message", data);
      return;
    }
    listenersRef.current.forEach((cb) => cb(data));
  });
  conn.on("close", () => setStatus("disconnected"));
  conn.on("error", (e) => {
    setError(e.message ?? String(e));
    setStatus("disconnected");
  });
}

export function useHostPeer(): UsePeerConnectionResult {
  const [status, setStatus] = useState<PeerStatus>("init");
  const [peerId, setPeerId] = useState<string | null>(null);
  const [error, setError] = useState<string | null>(null);
  const peerRef = useRef<Peer | null>(null);
  const connRef = useRef<DataConnection | null>(null);
  const listenersRef = useRef<Set<(msg: PeerMessage) => void>>(new Set());

  useEffect(() => {
    const peer = new Peer();
    peerRef.current = peer;

    peer.on("open", (id) => {
      setPeerId(id);
      setStatus("awaiting");
    });

    peer.on("connection", (conn) => {
      if (connRef.current) {
        conn.close();
        return;
      }
      connRef.current = conn;
      bindConnection(conn, listenersRef, setStatus, setError);
    });

    peer.on("error", (e) => {
      setError(e.message ?? String(e));
      setStatus("failed");
    });

    return () => {
      peer.destroy();
      peerRef.current = null;
      connRef.current = null;
    };
  }, []);

  const send = useCallback((msg: PeerMessage) => {
    const conn = connRef.current;
    if (!conn || !conn.open) {
      console.warn("[p2p] send before ready", msg);
      return;
    }
    conn.send(msg);
  }, []);

  const onMessage = useCallback((handler: (msg: PeerMessage) => void) => {
    listenersRef.current.add(handler);
    return () => {
      listenersRef.current.delete(handler);
    };
  }, []);

  return { status, peerId, error, send, onMessage };
}

export function useJoinPeer(remotePeerId: string): UsePeerConnectionResult {
  const [status, setStatus] = useState<PeerStatus>("init");
  const [error, setError] = useState<string | null>(null);
  const peerRef = useRef<Peer | null>(null);
  const connRef = useRef<DataConnection | null>(null);
  const listenersRef = useRef<Set<(msg: PeerMessage) => void>>(new Set());

  useEffect(() => {
    const peer = new Peer();
    peerRef.current = peer;

    peer.on("open", () => {
      setStatus("dialing");
      const conn = peer.connect(remotePeerId);
      connRef.current = conn;
      bindConnection(conn, listenersRef, setStatus, setError);
    });

    peer.on("error", (e) => {
      setError(e.message ?? String(e));
      setStatus("failed");
    });

    return () => {
      peer.destroy();
      peerRef.current = null;
      connRef.current = null;
    };
  }, [remotePeerId]);

  const send = useCallback((msg: PeerMessage) => {
    const conn = connRef.current;
    if (!conn || !conn.open) {
      console.warn("[p2p] send before ready", msg);
      return;
    }
    conn.send(msg);
  }, []);

  const onMessage = useCallback((handler: (msg: PeerMessage) => void) => {
    listenersRef.current.add(handler);
    return () => {
      listenersRef.current.delete(handler);
    };
  }, []);

  return { status, peerId: null, error, send, onMessage };
}
```

- [ ] **Step 4: 跑测试，确认 PASS**

```bash
npm test
```

Expected: 全部通过。

- [ ] **Step 5: tsc 校验**

```bash
npx tsc -b --noEmit
```

Expected: 无错误。

- [ ] **Step 6: 提交**

```bash
git add src/p2p/usePeerConnection.ts src/p2p/usePeerConnection.test.tsx
git commit -m "feat(p2p): add useHostPeer and useJoinPeer hooks"
```

---

### Task 9: 创建 `InviteModal.tsx`

**Files:**
- Create: `src/p2p/InviteModal.tsx`
- Create: `src/p2p/InviteModal.css`

- [ ] **Step 1: 写组件**

```tsx
// src/p2p/InviteModal.tsx
import { useMemo, useState } from "react";
import { buildInviteUrl, type PlayerRole } from "./protocol";
import "./InviteModal.css";

interface InviteModalProps {
  hostPeerId: string | null;          // PeerJS broker 给的本端 ID
  hostStatus: "awaiting" | "connected";
  onChooseRole: (hostRole: PlayerRole) => void;
  hostRole: PlayerRole | null;        // 已选阵营，未选为 null
  onCancel: () => void;
}

export default function InviteModal({
  hostPeerId,
  hostStatus,
  onChooseRole,
  hostRole,
  onCancel,
}: InviteModalProps) {
  const [copied, setCopied] = useState(false);

  const inviteUrl = useMemo(() => {
    if (!hostPeerId || !hostRole) return "";
    const joinerRole: PlayerRole = hostRole === "A" ? "B" : "A";
    return buildInviteUrl(window.location.origin, hostPeerId, joinerRole);
  }, [hostPeerId, hostRole]);

  const handleCopy = async () => {
    if (!inviteUrl) return;
    try {
      await navigator.clipboard.writeText(inviteUrl);
      setCopied(true);
      setTimeout(() => setCopied(false), 1500);
    } catch {
      // 不支持 clipboard 时退化为提示手动选中复制
      alert("请手动选中链接复制");
    }
  };

  // Step 1: 选阵营
  if (!hostRole) {
    return (
      <div className="invite-modal-backdrop">
        <div className="invite-modal">
          <h3>选择阵营</h3>
          <div className="invite-role-row">
            <button className="invite-role invite-role-blue" onClick={() => onChooseRole("A")}>
              我玩蓝方（先手）
            </button>
            <button className="invite-role invite-role-red" onClick={() => onChooseRole("B")}>
              我玩红方
            </button>
          </div>
          <button className="invite-cancel" onClick={onCancel}>
            取消
          </button>
        </div>
      </div>
    );
  }

  // Step 2: 展示邀请链接
  return (
    <div className="invite-modal-backdrop">
      <div className="invite-modal">
        <h3>邀请对方加入</h3>
        <p className="invite-hint">把这条链接发给你的对手：</p>
        <input className="invite-url" readOnly value={inviteUrl || "正在连接信令服务器..."} />
        <div className="invite-actions">
          <button onClick={handleCopy} disabled={!inviteUrl}>
            {copied ? "已复制 ✓" : "复制链接"}
          </button>
        </div>
        <p className="invite-status">
          {hostStatus === "awaiting" ? "等待对方加入..." : "对方已连接，进入对战"}
        </p>
        <button className="invite-cancel" onClick={onCancel}>
          取消
        </button>
      </div>
    </div>
  );
}
```

- [ ] **Step 2: 写 CSS（极简，复用现有配色）**

```css
/* src/p2p/InviteModal.css */
.invite-modal-backdrop {
  position: fixed;
  inset: 0;
  background: rgba(0, 0, 0, 0.55);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 1000;
}

.invite-modal {
  background: #1f1f1f;
  color: #fafafa;
  padding: 1.5rem 1.75rem;
  border-radius: 10px;
  min-width: 320px;
  max-width: 90vw;
  box-shadow: 0 8px 30px rgba(0, 0, 0, 0.45);
  text-align: center;
}

.invite-modal h3 {
  margin: 0 0 1rem;
}

.invite-role-row {
  display: flex;
  gap: 0.75rem;
  justify-content: center;
  margin-bottom: 1rem;
}

.invite-role {
  flex: 1;
  padding: 0.7rem 1rem;
  border: none;
  border-radius: 8px;
  font-size: 1rem;
  cursor: pointer;
  color: #fff;
}

.invite-role-blue { background: #1976d2; }
.invite-role-red { background: #d32f2f; }
.invite-role:hover { filter: brightness(1.15); }

.invite-hint {
  margin: 0.25rem 0 0.5rem;
  opacity: 0.85;
}

.invite-url {
  width: 100%;
  padding: 0.5rem 0.6rem;
  border-radius: 6px;
  border: 1px solid #444;
  background: #2c2c2c;
  color: #fafafa;
  font-family: monospace;
  font-size: 0.85rem;
  box-sizing: border-box;
}

.invite-actions {
  margin: 0.6rem 0;
}

.invite-actions button {
  padding: 0.45rem 1rem;
  border-radius: 6px;
  border: none;
  background: #2e7d32;
  color: #fff;
  cursor: pointer;
}

.invite-actions button:disabled {
  opacity: 0.5;
  cursor: not-allowed;
}

.invite-status {
  margin: 0.5rem 0;
  opacity: 0.85;
  font-size: 0.9rem;
}

.invite-cancel {
  margin-top: 0.5rem;
  background: transparent;
  border: 1px solid #555;
  color: #ddd;
  padding: 0.35rem 0.9rem;
  border-radius: 6px;
  cursor: pointer;
}
```

- [ ] **Step 3: tsc 校验**

```bash
npx tsc -b --noEmit
```

- [ ] **Step 4: 提交**

```bash
git add src/p2p/InviteModal.tsx src/p2p/InviteModal.css
git commit -m "feat(p2p): add InviteModal for role selection and link sharing"
```

---

### Task 10: 改造 `App.tsx` 路由识别 `#game?...`

**Files:**
- Modify: `src/App.tsx`

- [ ] **Step 1: 把所有 `=== '#game'` 改为 `.startsWith('#game')`**

`src/App.tsx` 中两处：
1. `useState(() => window.location.hash === '#game')`
2. `setShowGame(window.location.hash === '#game')`

改为：

```tsx
const [showGame, setShowGame] = useState(() => window.location.hash.startsWith('#game'))
// ...
const onHashChange = () => {
  setShowGame(window.location.hash.startsWith('#game'))
}
```

- [ ] **Step 2: 改写 `handleBackClick` 确保返回简历页时清掉所有 hash**

```tsx
const handleBackClick = useCallback(() => {
  setTransitioning(true)
  setTimeout(() => {
    history.replaceState(null, '', window.location.pathname)
    setShowGame(false)
    setTransitioning(false)
  }, 300)
}, [])
```

（已是该形态，保留即可。）

- [ ] **Step 3: tsc 校验 + 手测**

```bash
npx tsc -b --noEmit
npm run dev
```

打开：
- `http://localhost:5173/` → 进简历
- `http://localhost:5173/#game` → 进游戏（单机模式不变）
- `http://localhost:5173/#game?peer=fakeid&role=red` → 进游戏（应进入 GameApp，目前还没接入 joiner，所以会渲染单机但 URL 命中）

- [ ] **Step 4: 提交**

```bash
git add src/App.tsx
git commit -m "feat(app): match #game with optional query for P2P invites"
```

---

### Task 11: `Game.tsx` 加入 mode 状态机（仅主机路径，先打通发起方）

**Files:**
- Modify: `src/Game.tsx`

- [ ] **Step 1: 重写 `Game.tsx`**

```tsx
import { useCallback, useState } from "react";
import GameContainer from "./game/GameContainer";
import InviteModal from "./p2p/InviteModal";
import { useHostPeer } from "./p2p/usePeerConnection";
import type { PlayerRole } from "./p2p/protocol";

type Mode =
  | { kind: "single" }
  | { kind: "host-setup"; role: PlayerRole | null }
  | { kind: "host-active"; role: PlayerRole };

export default function GameApp() {
  const [mode, setMode] = useState<Mode>({ kind: "single" });

  if (mode.kind === "single") {
    return (
      <GameContainer
        extraControls={
          <button
            id="p2pBtn"
            style={{ marginLeft: 8 }}
            onClick={() => setMode({ kind: "host-setup", role: null })}
          >
            🔗 P2P 对战
          </button>
        }
      />
    );
  }

  if (mode.kind === "host-setup") {
    return (
      <HostSetup
        chosen={mode.role}
        onChoose={(role) => setMode({ kind: "host-setup", role })}
        onConnected={(role) => setMode({ kind: "host-active", role })}
        onCancel={() => setMode({ kind: "single" })}
      />
    );
  }

  return (
    <HostActiveGame
      role={mode.role}
      onLeave={() => setMode({ kind: "single" })}
    />
  );
}

interface HostSetupProps {
  chosen: PlayerRole | null;
  onChoose: (r: PlayerRole) => void;
  onConnected: (r: PlayerRole) => void;
  onCancel: () => void;
}

function HostSetup({ chosen, onChoose, onConnected, onCancel }: HostSetupProps) {
  const host = useHostPeer();

  // 连上后通知父组件切到 active
  if (chosen && host.status === "connected") {
    onConnected(chosen);
  }

  return (
    <>
      <GameContainer />
      <InviteModal
        hostPeerId={host.peerId}
        hostStatus={host.status === "connected" ? "connected" : "awaiting"}
        hostRole={chosen}
        onChooseRole={onChoose}
        onCancel={onCancel}
      />
    </>
  );
}

interface HostActiveProps {
  role: PlayerRole;
  onLeave: () => void;
}

// 占位实现：先让单机棋盘渲染，下一任务接入 transport
function HostActiveGame({ role, onLeave }: HostActiveProps) {
  return (
    <GameContainer
      topBanner={
        <div style={{ fontSize: 14, opacity: 0.85, marginBottom: 4 }}>
          P2P 对战中 | 你是{role === "A" ? "蓝方" : "红方"}
          <button style={{ marginLeft: 12 }} onClick={onLeave}>返回单机</button>
        </div>
      }
    />
  );
}
```

> **注：** 上面 `if (chosen && host.status === "connected") { onConnected(chosen) }` 写在渲染中是反模式（会触发警告/无限循环）。下一步用 useEffect 修正。

- [ ] **Step 2: 用 `useEffect` 修正连接成功的状态切换**

在 `HostSetup` 内：

```tsx
import { useEffect } from "react";

function HostSetup({ chosen, onChoose, onConnected, onCancel }: HostSetupProps) {
  const host = useHostPeer();

  useEffect(() => {
    if (chosen && host.status === "connected") {
      onConnected(chosen);
    }
  }, [chosen, host.status, onConnected]);

  return (
    <>
      <GameContainer />
      <InviteModal
        hostPeerId={host.peerId}
        hostStatus={host.status === "connected" ? "connected" : "awaiting"}
        hostRole={chosen}
        onChooseRole={onChoose}
        onCancel={onCancel}
      />
    </>
  );
}
```

- [ ] **Step 3: tsc 校验 + 手测**

```bash
npx tsc -b --noEmit
npm run dev
```

打开 `http://localhost:5173/#game` → 棋盘下方/或棋盘附近应有"🔗 P2P 对战"按钮 → 点击 → 弹出阵营选择 → 选蓝方 → 显示邀请链接（peerId 由 broker 给出，等待对方）。

> 此时无法真正连上（joiner 还没实现），可在另一个标签页打开链接，会看到单机棋盘（说明 URL 路由正确），但 host 的状态不会切到 connected——这是预期。

- [ ] **Step 4: 提交**

```bash
git add src/Game.tsx
git commit -m "feat(p2p): wire host setup flow with InviteModal"
```

---

### Task 12: 接入 joiner 路径

**Files:**
- Modify: `src/Game.tsx`

- [ ] **Step 1: 在 `GameApp` 启动时解析 hash，自动进入 joiner 模式**

```tsx
import { useCallback, useEffect, useState } from "react";
import GameContainer from "./game/GameContainer";
import InviteModal from "./p2p/InviteModal";
import { useHostPeer, useJoinPeer } from "./p2p/usePeerConnection";
import { parseInviteHash, type PlayerRole } from "./p2p/protocol";

type Mode =
  | { kind: "single" }
  | { kind: "host-setup"; role: PlayerRole | null }
  | { kind: "host-active"; role: PlayerRole }
  | { kind: "join-pending"; remotePeerId: string; role: PlayerRole }
  | { kind: "join-active"; remotePeerId: string; role: PlayerRole };

export default function GameApp() {
  const [mode, setMode] = useState<Mode>(() => {
    const params = parseInviteHash(window.location.hash);
    if (params) {
      return { kind: "join-pending", remotePeerId: params.peerId, role: params.role };
    }
    return { kind: "single" };
  });

  const handleLeave = useCallback(() => {
    history.replaceState(null, "", "#game");
    setMode({ kind: "single" });
  }, []);

  if (mode.kind === "single") {
    return (
      <GameContainer
        extraControls={
          <button
            id="p2pBtn"
            style={{ marginLeft: 8 }}
            onClick={() => setMode({ kind: "host-setup", role: null })}
          >
            🔗 P2P 对战
          </button>
        }
      />
    );
  }

  if (mode.kind === "host-setup") {
    return (
      <HostSetup
        chosen={mode.role}
        onChoose={(role) => setMode({ kind: "host-setup", role })}
        onConnected={(role) => setMode({ kind: "host-active", role })}
        onCancel={handleLeave}
      />
    );
  }

  if (mode.kind === "host-active") {
    return <HostActiveGame role={mode.role} onLeave={handleLeave} />;
  }

  if (mode.kind === "join-pending" || mode.kind === "join-active") {
    return (
      <JoinerFlow
        remotePeerId={mode.remotePeerId}
        role={mode.role}
        onConnected={() =>
          setMode((prev) =>
            prev.kind === "join-pending"
              ? { kind: "join-active", remotePeerId: prev.remotePeerId, role: prev.role }
              : prev
          )
        }
        onLeave={handleLeave}
      />
    );
  }

  return null;
}

// HostSetup / HostActiveGame 保持 Task 11 的实现（略，不要删）

interface JoinerFlowProps {
  remotePeerId: string;
  role: PlayerRole;
  onConnected: () => void;
  onLeave: () => void;
}

function JoinerFlow({ remotePeerId, role, onConnected, onLeave }: JoinerFlowProps) {
  const join = useJoinPeer(remotePeerId);

  useEffect(() => {
    if (join.status === "connected") onConnected();
  }, [join.status, onConnected]);

  if (join.status === "failed") {
    return (
      <div style={{ textAlign: "center", marginTop: 40 }}>
        <p style={{ color: "#e57373" }}>连接失败，请联系发起方重新分享链接</p>
        <button onClick={onLeave}>返回单机</button>
      </div>
    );
  }

  if (join.status !== "connected") {
    return (
      <div style={{ textAlign: "center", marginTop: 40 }}>
        <p>正在连接对方...</p>
        <button onClick={onLeave}>取消</button>
      </div>
    );
  }

  return (
    <GameContainer
      topBanner={
        <div style={{ fontSize: 14, opacity: 0.85, marginBottom: 4 }}>
          P2P 对战中 | 你是{role === "A" ? "蓝方" : "红方"}
          <button style={{ marginLeft: 12 }} onClick={onLeave}>返回单机</button>
        </div>
      }
    />
  );
}
```

> 此时 Host/Joiner 都能跑通"连上"事件，但棋盘还没有同步（transport 还没接入 GameContainer）。

- [ ] **Step 2: 手测**

```bash
npm run dev
```

- 标签页 1 打开 `http://localhost:5173/#game` → 点 P2P 对战 → 选蓝方 → 复制链接
- 标签页 2 打开复制的链接 → 应自动进入 join 流程 → 几秒内 host 标签页 InviteModal 关闭、joiner 标签页"正在连接对方..."消失，两端各自渲染棋盘和顶部"P2P 对战中 | 你是..."条
- 此时落子 **不会** 同步（待 Task 13）

- [ ] **Step 3: 提交**

```bash
git add src/Game.tsx
git commit -m "feat(p2p): add joiner flow auto-triggered by invite hash"
```

---

### Task 13: 把 transport 接入 `GameContainer`（同步落子和 reset，禁用悔棋）

**Files:**
- Modify: `src/game/GameContainer.tsx`
- Modify: `src/Game.tsx`

- [ ] **Step 1: 扩展 `GameContainerProps`**

在 `src/game/GameContainer.tsx`：

```tsx
import type { PeerMessage, PlayerRole } from "../p2p/protocol";

export interface GameContainerTransport {
  send: (msg: PeerMessage) => void;
  onMessage: (handler: (msg: PeerMessage) => void) => () => void;
  localRole: PlayerRole;
}

export interface GameContainerProps {
  extraControls?: React.ReactNode;
  topBanner?: React.ReactNode;
  transport?: GameContainerTransport;
}
```

- [ ] **Step 2: 在 `GameContainer` 中根据 transport 改变行为**

完整新版 `GameContainer.tsx`：

```tsx
import { useCallback, useEffect, useRef, useState } from "react";
import constants from "../constants";
import { applyMove, applyReset, applyUndo, createInitialState } from "./gameLogic";
import type { GameState, Winner } from "./types";
import GameView from "./GameView";
import type { PeerMessage, PlayerRole } from "../p2p/protocol";

function playWinSound(winner: Winner) {
  const soundMap: Record<number, string> = {
    [constants.PLAYER_A]: "./tansonwin.mp3",
    [constants.PLAYER_B]: "./sherlywin.mp3",
    3: "./allwin.mp3",
  };
  const url = soundMap[winner];
  if (url) {
    const audio = new Audio(url);
    audio.volume = 0.25;
    audio.play().catch(() => {});
  }
}

type AvatarSlot = {
  name: "tanson" | "sherly";
  level: number;
  setLevel: (n: number) => void;
};

function probeNextLevel(slot: AvatarSlot): Promise<boolean> {
  return new Promise((resolve) => {
    const img = new Image();
    img.onload = () => resolve(true);
    img.onerror = () => resolve(false);
    img.src = `/${slot.name}_${slot.level + 1}.jpg`;
  });
}

async function tryUpgradeOrReset(winner: AvatarSlot, loser: AvatarSlot): Promise<void> {
  const winnerHasNext = await probeNextLevel(winner);
  if (winnerHasNext) {
    winner.setLevel(winner.level + 1);
    return;
  }
  const loserHasNext = await probeNextLevel(loser);
  if (!loserHasNext) {
    winner.setLevel(0);
    loser.setLevel(0);
  }
}

export interface GameContainerTransport {
  send: (msg: PeerMessage) => void;
  onMessage: (handler: (msg: PeerMessage) => void) => () => void;
  localRole: PlayerRole;
}

export interface GameContainerProps {
  extraControls?: React.ReactNode;
  topBanner?: React.ReactNode;
  transport?: GameContainerTransport;
}

export default function GameContainer({ extraControls, topBanner, transport }: GameContainerProps) {
  const [state, setState] = useState<GameState>(() => createInitialState(true));
  const [aLevel, setALevel] = useState(0);
  const [bLevel, setBLevel] = useState(0);
  const seqRef = useRef(0);

  useEffect(() => {
    if (state.winner === undefined) return;
    playWinSound(state.winner);
    if (state.winner === 3) return;
    const slotA: AvatarSlot = { name: "tanson", level: aLevel, setLevel: setALevel };
    const slotB: AvatarSlot = { name: "sherly", level: bLevel, setLevel: setBLevel };
    if (state.winner === constants.PLAYER_A) void tryUpgradeOrReset(slotA, slotB);
    else void tryUpgradeOrReset(slotB, slotA);
  }, [state.winner]);

  useEffect(() => {
    if (state.droppingCell === null) return;
    const t = setTimeout(() => {
      setState((s) => (s.droppingCell ? { ...s, droppingCell: null } : s));
    }, 400);
    return () => clearTimeout(t);
  }, [state.droppingCell]);

  // P2P：订阅对方消息
  useEffect(() => {
    if (!transport) return;
    const unsub = transport.onMessage((msg) => {
      if (msg.type === "move") {
        setState((s) => applyMove(s, msg.col));
      } else if (msg.type === "reset") {
        setState((s) => applyReset(s));
      }
    });
    return unsub;
  }, [transport]);

  const isLocalTurn = useCallback(() => {
    if (!transport) return true;
    const localIsA = transport.localRole === "A";
    return state.aTurn === localIsA;
  }, [state.aTurn, transport]);

  const handleColumnClick = useCallback(
    (col: number) => {
      if (transport && !isLocalTurn()) return;
      setState((s) => {
        const next = applyMove(s, col);
        if (next !== s && transport) {
          seqRef.current += 1;
          transport.send({ type: "move", col, seq: seqRef.current });
        }
        return next;
      });
    },
    [transport, isLocalTurn]
  );

  const handlePlayAgain = useCallback(() => {
    setState((s) => applyReset(s));
    if (transport) {
      seqRef.current += 1;
      transport.send({ type: "reset", seq: seqRef.current });
    }
  }, [transport]);

  const handleUndo = useCallback(() => {
    setState((s) => applyUndo(s));
  }, []);

  return (
    <GameView
      state={state}
      aLevel={aLevel}
      bLevel={bLevel}
      onColumnClick={handleColumnClick}
      onPlayAgain={handlePlayAgain}
      onUndo={transport ? undefined : handleUndo}
      extraControls={extraControls}
      topBanner={topBanner}
    />
  );
}
```

- [ ] **Step 3: 完整重写 `src/Game.tsx`，合并 host-setup/host-active**

> **设计说明**：Task 11/12 把 host 流程拆成 `host-setup`/`host-active` 两个 mode；但切换会让 `useHostPeer` 所在组件 unmount，进而触发 `peer.destroy()`，连接立即断开。正确做法是**合并为一个 `HostFlow` 组件**，里面用本地 state 控制 InviteModal 显隐（连上后隐藏 modal，棋盘继续渲染）。Task 11/12 的拆分写法在本任务被完全替换。

替换 `src/Game.tsx` 完整版：

```tsx
import { useCallback, useEffect, useMemo, useState } from "react";
import GameContainer from "./game/GameContainer";
import type { GameContainerTransport } from "./game/GameContainer";
import InviteModal from "./p2p/InviteModal";
import { useHostPeer, useJoinPeer, type UsePeerConnectionResult } from "./p2p/usePeerConnection";
import { parseInviteHash, type PlayerRole } from "./p2p/protocol";

type Mode =
  | { kind: "single" }
  | { kind: "host" }
  | { kind: "join"; remotePeerId: string; role: PlayerRole };

export default function GameApp() {
  const [mode, setMode] = useState<Mode>(() => {
    const params = parseInviteHash(window.location.hash);
    if (params) return { kind: "join", remotePeerId: params.peerId, role: params.role };
    return { kind: "single" };
  });

  const handleLeave = useCallback(() => {
    history.replaceState(null, "", "#game");
    setMode({ kind: "single" });
  }, []);

  if (mode.kind === "single") {
    return (
      <GameContainer
        extraControls={
          <button id="p2pBtn" style={{ marginLeft: 8 }} onClick={() => setMode({ kind: "host" })}>
            🔗 P2P 对战
          </button>
        }
      />
    );
  }

  if (mode.kind === "host") {
    return <HostFlow onLeave={handleLeave} />;
  }

  return <JoinerFlow remotePeerId={mode.remotePeerId} role={mode.role} onLeave={handleLeave} />;
}

function HostFlow({ onLeave }: { onLeave: () => void }) {
  const host = useHostPeer();
  const [hostRole, setHostRole] = useState<PlayerRole | null>(null);
  const transport = useTransport(host, hostRole);

  const showModal = host.status !== "connected";

  return (
    <>
      <GameContainer
        transport={transport}
        topBanner={
          host.status === "connected" && hostRole ? (
            <ConnectionBanner localRole={hostRole} status={host.status} onLeave={onLeave} />
          ) : null
        }
      />
      {showModal && (
        <InviteModal
          hostPeerId={host.peerId}
          hostStatus="awaiting"
          hostRole={hostRole}
          onChooseRole={setHostRole}
          onCancel={onLeave}
        />
      )}
    </>
  );
}

interface JoinerFlowProps {
  remotePeerId: string;
  role: PlayerRole;
  onLeave: () => void;
}

function JoinerFlow({ remotePeerId, role, onLeave }: JoinerFlowProps) {
  const join = useJoinPeer(remotePeerId);
  const transport = useTransport(join, role);

  if (join.status === "failed") {
    return (
      <div style={{ textAlign: "center", marginTop: 40 }}>
        <p style={{ color: "#e57373" }}>连接失败，请联系发起方重新分享链接</p>
        <button onClick={onLeave}>返回单机</button>
      </div>
    );
  }

  if (join.status !== "connected") {
    return (
      <div style={{ textAlign: "center", marginTop: 40 }}>
        <p>正在连接对方...</p>
        <button onClick={onLeave}>取消</button>
      </div>
    );
  }

  return (
    <GameContainer
      transport={transport}
      topBanner={<ConnectionBanner localRole={role} status={join.status} onLeave={onLeave} />}
    />
  );
}

function useTransport(
  peer: UsePeerConnectionResult,
  localRole: PlayerRole | null
): GameContainerTransport | undefined {
  return useMemo(() => {
    if (!localRole) return undefined;
    if (peer.status !== "connected") return undefined;
    return {
      send: peer.send,
      onMessage: peer.onMessage,
      localRole,
    };
  }, [peer.status, peer.send, peer.onMessage, localRole]);
}

interface ConnectionBannerProps {
  localRole: PlayerRole;
  status: string;
  onLeave: () => void;
}

function ConnectionBanner({ localRole, status, onLeave }: ConnectionBannerProps) {
  const colorText = localRole === "A" ? "蓝方" : "红方";
  const dotColor = status === "connected" ? "#4caf50" : "#e57373";
  return (
    <div style={{ fontSize: 14, opacity: 0.9, marginBottom: 6 }}>
      <span style={{ display: "inline-block", width: 8, height: 8, background: dotColor, borderRadius: 4, marginRight: 6 }} />
      P2P 对战中 | 你是 {colorText}
      <button style={{ marginLeft: 12 }} onClick={onLeave}>返回单机</button>
    </div>
  );
}
```

- [ ] **Step 4: 同步导出 `UsePeerConnectionResult`（如果尚未导出）**

在 `src/p2p/usePeerConnection.ts` 顶部应已 `export interface UsePeerConnectionResult`，确认它对外可见。

- [ ] **Step 5: tsc 校验 + 双标签手测**

```bash
npx tsc -b --noEmit
npm run dev
```

- 标签页 1：`#game` → P2P 对战 → 选蓝方 → 复制链接
- 标签页 2：粘贴链接 → 进入 → "正在连接..." → 连上 → 双方都看到棋盘
- 标签页 1（蓝方=A=先手）落第一子 → 标签页 2 同步落子，且不能在自己回合外乱点
- 标签页 2 落子 → 标签页 1 同步
- 一方胜利 → 双方都看到 Crown 和胜利音效
- 任一方点"再玩一次" → 双方同步重开
- P2P 模式下 **无悔棋按钮**（验证：Crown 没出现时按钮区无"悔棋"）
- 任一方点"返回单机" → 自己回单机；对方应在几秒内看到顶部 banner 变红 / 进入 disconnected 处理（待 Task 14 改善）

- [ ] **Step 6: 提交**

```bash
git add src/game/GameContainer.tsx src/Game.tsx
git commit -m "feat(p2p): sync moves and reset over data channel; disable undo in P2P"
```

---

### Task 14: 断线提示与错误 UI

**Files:**
- Modify: `src/Game.tsx`

- [ ] **Step 1: `ConnectionBanner` 增加 disconnected/failed 红条**

替换 `ConnectionBanner` 实现：

```tsx
function ConnectionBanner({ localRole, status, onLeave }: ConnectionBannerProps) {
  const colorText = localRole === "A" ? "蓝方" : "红方";
  const disconnected = status === "disconnected" || status === "failed";
  return (
    <div
      style={{
        fontSize: 14,
        marginBottom: 6,
        padding: disconnected ? "6px 10px" : 0,
        background: disconnected ? "rgba(229,115,115,0.18)" : "transparent",
        borderRadius: 6,
        color: disconnected ? "#e57373" : "inherit",
      }}
    >
      <span
        style={{
          display: "inline-block",
          width: 8,
          height: 8,
          background: disconnected ? "#e57373" : "#4caf50",
          borderRadius: 4,
          marginRight: 6,
        }}
      />
      {disconnected ? "对方已断开" : `P2P 对战中 | 你是 ${colorText}`}
      <button style={{ marginLeft: 12 }} onClick={onLeave}>
        返回单机
      </button>
    </div>
  );
}
```

- [ ] **Step 2: `HostFlow` 在连上之后即使断开也保留 GameContainer + 红条**

调整 `HostFlow`，让 `topBanner` 在 host.status 为 connected 或 disconnected/failed 时都显示 banner（hostRole 已设置才显示）：

```tsx
function HostFlow({ onLeave }: { onLeave: () => void }) {
  const host = useHostPeer();
  const [hostRole, setHostRole] = useState<PlayerRole | null>(null);
  const transport = useTransport(host, hostRole);
  const hasConnectedOnce = host.status === "connected" || host.status === "disconnected";
  const showModal = !hasConnectedOnce;

  return (
    <>
      <GameContainer
        transport={transport}
        topBanner={
          hostRole && hasConnectedOnce ? (
            <ConnectionBanner localRole={hostRole} status={host.status} onLeave={onLeave} />
          ) : null
        }
      />
      {showModal && (
        <InviteModal
          hostPeerId={host.peerId}
          hostStatus="awaiting"
          hostRole={hostRole}
          onChooseRole={setHostRole}
          onCancel={onLeave}
        />
      )}
    </>
  );
}
```

同理 `JoinerFlow` 在 connected 后即使 disconnect 也保留棋盘：

```tsx
function JoinerFlow({ remotePeerId, role, onLeave }: JoinerFlowProps) {
  const join = useJoinPeer(remotePeerId);
  const transport = useTransport(join, role);
  const [hasConnected, setHasConnected] = useState(false);

  useEffect(() => {
    if (join.status === "connected") setHasConnected(true);
  }, [join.status]);

  if (join.status === "failed" && !hasConnected) {
    return (
      <div style={{ textAlign: "center", marginTop: 40 }}>
        <p style={{ color: "#e57373" }}>连接失败，请联系发起方重新分享链接</p>
        <button onClick={onLeave}>返回单机</button>
      </div>
    );
  }

  if (!hasConnected) {
    return (
      <div style={{ textAlign: "center", marginTop: 40 }}>
        <p>正在连接对方...</p>
        <button onClick={onLeave}>取消</button>
      </div>
    );
  }

  return (
    <GameContainer
      transport={transport}
      topBanner={<ConnectionBanner localRole={role} status={join.status} onLeave={onLeave} />}
    />
  );
}
```

- [ ] **Step 3: tsc 校验 + 手测**

```bash
npx tsc -b --noEmit
npm run dev
```

双标签：连上 → 一方关闭标签 → 另一方顶部应在 ~5 秒内变红 "对方已断开"，"返回单机"按钮可用，棋盘保留。

- [ ] **Step 4: 提交**

```bash
git add src/Game.tsx
git commit -m "feat(p2p): show disconnect banner while preserving final board"
```

---

### Task 15: 全量回归 + manual E2E checklist

**Files:**
- 无文件改动；仅手测 + 文档化测试通过

- [ ] **Step 1: 跑全部单测**

```bash
npm test
```

Expected: 所有 `gameLogic`、`protocol`、`usePeerConnection` 测试通过。

- [ ] **Step 2: 跑 build**

```bash
npm run build
```

Expected: tsc + vite build 均无错。

- [ ] **Step 3: 单机模式完整手测**

`npm run dev`，打开 `#game`：

- [ ] 落子动画正常
- [ ] 胜利后 Crown 显示、音效播放、头像升级
- [ ] "再玩一次" 重置且交换先手
- [ ] "悔棋" 在没胜负时可用
- [ ] 平局（满盘）触发 winner=3 双 Crown

- [ ] **Step 4: P2P 模式两标签端到端**

- [ ] 标签页 1 选蓝方 → 链接生成 → 标签页 2 打开链接 → 自动连上
- [ ] 标签页 1 落子 → 标签页 2 同步显示同色子
- [ ] 标签页 2 落子 → 标签页 1 同步
- [ ] 任一方在对方回合点击棋盘 → 被忽略（不落子，不报错）
- [ ] 一方胜 → 双方 Crown + 音效 + 头像升级
- [ ] 任一方"再玩一次" → 双方同步清盘并交换先手
- [ ] P2P 模式两端均无"悔棋"按钮
- [ ] 一方关标签页 → 另一方顶部变红 "对方已断开"，可点"返回单机"

- [ ] **Step 5: 边界 / 异常手测**

- [ ] 第三个标签页打开同一邀请链接：joiner 看到 "连接失败" 或 host 拒绝（第二 conn 被关闭）
- [ ] 邀请链接被截断（去掉 `&role=`）：打开 → 进入单机模式（无 invalid hash 报错亦可接受）
- [ ] 控制台无未捕获异常、无 React 警告

- [ ] **Step 6: 提交（如有任何修复）**

如手测中发现问题：定位 → 补单测 → 修代码 → 提交。
若 100% 通过：

```bash
git tag p2p-mode-v1
git log --oneline -20
```

记录完整提交链，作为 release 标记。

---

## 自查清单（实施完成后）

- [ ] Spec 第 1 节（背景与目标）→ Task 11/12/13 覆盖
- [ ] Spec 第 2.1 节（PeerJS 信令）→ Task 7/8
- [ ] Spec 第 2.2 节（仅同步动作 / 禁用悔棋 / reset 双方同步）→ Task 13
- [ ] Spec 第 2.3 节（颜色映射 蓝=A=tanson）→ Task 6（protocol）+ Task 9（modal 文案）+ Task 13（banner 文案）
- [ ] Spec 第 3 节（文件结构方案 3）→ Task 1–5（拆出 game/）+ Task 6–9（p2p/）
- [ ] Spec 第 4 节（数据流）→ Task 13
- [ ] Spec 第 5 节（协议 + URL）→ Task 6
- [ ] Spec 第 6 节（PeerJS 生命周期）→ Task 8
- [ ] Spec 第 7 节（UI 流程）→ Task 9 + Task 11 + Task 12 + Task 14
- [ ] Spec 第 8 节（错误处理与边界）→ Task 14 + Task 15
- [ ] Spec 第 9 节（测试策略）→ Task 0 + Task 2 + Task 6 + Task 8 + Task 15
- [ ] Spec 第 10 节（依赖）→ Task 7
- [ ] Spec 第 11/12 节（实施顺序 / YAGNI）→ 全计划遵循

## 关键设计要点（实施时务必遵守）

1. **Transport 接口要稳定**：`{send, onMessage, localRole}`，GameContainer 不应直接依赖 `useHostPeer`/`useJoinPeer`，便于测试时注入 mock。
2. **Peer 实例生命周期**：`HostFlow` 持有 `useHostPeer`；切换到对战阶段时**不要卸载** HostFlow（避免 peer.destroy 断开连接）。Task 13 已合并 setup+active 为同一组件解决。
3. **轮次校验是 UX 防误触，不是反作弊**：本地点击在自己回合外被忽略；但收到对方非法消息（满列、跳号）只警告不崩溃。
4. **`droppingCell` 动画清理**：用 `useEffect` 监听 droppingCell，定时清掉。原版 setTimeout 内嵌 setState 在拆出 reducer 后已不可行，必须用 effect。
5. **URL 复用 `#game`**：所有 hash 路由依赖 `startsWith('#game')`，不要漏改。
6. **不要持久化 P2P state**：刷新即重置（除非 URL 仍带邀请参数，那是 joiner 重新加入）。
